{
	"$schema": "./meta/extended-type.json",
	"extendsType": "User identifier",
	"typeKey": "type",
	"types": {
		"m.id.phone": {
			"type": "object",
			"properties": {
				"country": {
					"type": "string",
					"description": "A two letter uppercase ISO-3166-1 alpha-2 country code"
				},
				"phone": {
					"type": "string",
					"description": "A phone number, to be canonicalized by the server"
				}
			},
			"required": [
				"country",
				"phone"
			]
		},
		"m.id.user": {
			"type": "object",
			"properties": {
				"user": {
					"type": "string",
					"description": "A client can identify a user using their Matrix ID. This can either be the fully qualified Matrix user ID, or just the localpart of the user ID."
				}
			},
			"required": [
				"user"
			]
		}
	},
	"type": "extended-type"
}
